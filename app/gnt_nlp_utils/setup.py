from setuptools import setup, find_packages

setup(name='gnt-nlp-utils',
      version='1.0',
      description='NLP utils for SBLGNT clusterer application',
      author='Sophie Robert',
      find_packages=find_packages()
      )
