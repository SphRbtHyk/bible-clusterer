from setuptools import setup, find_packages

setup(name='gnt-api',
      version='1.0',
      description='API for SBLGNT clusterer application',
      author='Sophie Robert',
      find_packages=find_packages()
      )
