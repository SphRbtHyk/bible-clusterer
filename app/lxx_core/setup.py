from setuptools import setup, find_packages

setup(name='lxx-core',
      version='1.0',
      description='Core for LXX clusterer application',
      author='Sophie Robert',
      find_packages=find_packages()
      )
