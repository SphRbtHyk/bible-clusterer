from setuptools import setup, find_packages

setup(name='clusterer-api',
      version='1.0',
      description='API for bible clusterer application',
      author='Sophie Robert',
      find_packages=find_packages()
      )
