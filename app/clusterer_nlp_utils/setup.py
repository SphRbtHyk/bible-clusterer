from setuptools import setup, find_packages

setup(name='bible-nlp-utils',
      version='1.0',
      description='NLP utils for Bible clusterer application',
      author='Sophie Robert',
      find_packages=find_packages()
      )
