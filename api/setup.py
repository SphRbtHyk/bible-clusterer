from setuptools import setup, find_packages

setup(name='lxx-api',
      version='1.0',
      description='API for LXX clusterer application',
      author='Sophie Robert',
      find_packages=find_packages()
      )
